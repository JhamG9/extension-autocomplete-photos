// document.addEventListener("DOMContentLoaded", () => {
//   const targetElement = document.querySelector('body');

//   // Ver el nodo del elemento en la consola
//   console.log(targetElement);

//   // Mostrar el contenido del body como un string en un alert
//   alert(targetElement.innerHTML);  // Esto traerá todo el HTML dentro del body

//   // También puedes mostrar solo el texto sin etiquetas HTML
//   // alert(targetElement.textContent);

//   if (targetElement) {
//     console.log("Elemento encontrado:", targetElement.textContent);  // Imprime el texto del body
//   } else {
//     console.log("No se encontró el elemento.");
//   }
// });